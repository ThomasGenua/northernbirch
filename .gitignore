node_modules/
.env
.env.local
.env.*.local
*.db
*.db-wal
*.db-shm
build/
.DS_Store
uploads/
backups/
*.log
.vscode/
.idea/
coverage/
.nyc_output/
# Capacitor native projects - generated by cap add
mobile/ios/
mobile/android/

# Build output. Netlify runs `npm run build` itself (netlify.toml), so the
# deployed site never came from these files -- committing them only put a
# regenerated copy of every page in every diff.
dist/
dist-ssr/
